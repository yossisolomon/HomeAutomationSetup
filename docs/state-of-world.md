# Smart Home — State of the World

> **Purpose:** Current snapshot of the live setup. For automation design, see
> [`docs/automation-architecture.md`](automation-architecture.md) and the autogenerated
> registry at [`docs/automations.md`](automations.md).

---

## 1. Project & Apartment

- **Owner:** computer engineer; comfortable with Linux/Docker/YAML.
- **Goals:** low-maintenance, high Wife Acceptance Factor (WAF).
- **Apartment:** 125 sqm + 38 sqm west/southwest porch, 13th floor, Kiryat Motzkin (northern Israel).
- **Network:** Asus mesh Wi-Fi — main router at main entrance, secondary node in Bedroom 3.
- **Mamad (Bedroom 2 / toddler's room):** reinforced-concrete safe room in the center of the apartment. Acts as a Faraday cage for RF and Bluetooth. **2.4 GHz Wi-Fi penetrates it even when locked** — Wi-Fi devices inside are reachable.

### Room map

| Zone | Room | Notes |
|------|------|-------|
| Entrance wing | Main Entrance | Asus main router |
| Entrance wing | Guest Bathroom | Windowless; electric vent fan + 2 kW heater |
| Entrance wing | Bedroom 4 | Dedicated Tornado A/C + RF ceiling fan |
| Center | Living Room + Kitchen | Open-concept; central A/C (Tornado Mini-Central) |
| Center | Porch (38 sqm) | Two RF fan/lights |
| Center | Service Balcony | Boiler (Dood), A/C compressors |
| Bedroom corridor | Mamad / Bedroom 2 | Toddler's room; concrete safe room |
| Bedroom corridor | Bedroom 3 | Houses Asus secondary mesh node |
| Bedroom corridor | Master Bedroom | En-suite bathroom with window |
| End of corridor | Main Bathroom | Washing machine + heat-pump dryer; window |

---

## 2. Live Infrastructure

### Server — `blacky`

| Item | Detail |
|------|--------|
| Hardware | ThinkPad T400 |
| OS | Debian 13 headless |
| Runtime | Docker Compose |

**Services running:**

| Service | Role |
|---------|------|
| Home Assistant | Core automation platform |
| Node-RED | Advanced automations |
| Mosquitto | MQTT broker |
| ESPHome | ESP32 firmware management |
| zigbee2mqtt | Zigbee coordinator bridge |
| Grafana + Prometheus | Observability dashboards |
| Portainer | Container management UI |
| cAdvisor + node-exporter | Container + host metrics |

### Integrations & devices (live)

| Category | Device / Integration | Notes |
|----------|---------------------|-------|
| Air quality (outdoor) | WAQI — station `KMotzkin Begin – Haifa and Krayot` | On the owner's street |
| Air quality (kitchen/living) | Qingping Air Monitor **Pro** | Wi-Fi/cloud-MQTT; has VOC sensor |
| Air quality (master bedroom) | Qingping Air Monitor **Lite** | Wi-Fi/cloud-MQTT |
| Air purifier | Xiaomi Smart Air Purifier **4 Pro** (living room) | HACS Xiaomi MIoT Auto, local mode |
| Air purifier | Xiaomi Smart Air Purifier **4 Compact** (master bedroom) | Same integration |
| Ceiling fans | Broadlink RM4 Pro × 1 → 433 MHz RF, whole-house | 6 fan + 6 light template entities in `config/template/fans.yaml` |
| Boiler | LocalTuya smart switch (Smartr 16A, double-pole) | Done |
| Messaging | Telegram bot | |
| Weather | Met.no | |
| Vacuum | Roborock | |
| TV | LG TV + Android TV | |
| Zigbee | zigbee2mqtt — **2 of 8 plant soil-moisture sensors paired** | |

---

## 3. Partial / Pending

| Item | Status |
|------|--------|
| Qingping Air Monitor **Lite** (3rd unit, for Mamad) | Shipped; not yet hooked up |
| Broadlink RM4 Pro (2nd unit, Bedroom 4 A/C) | Powered; not yet configured |
| Main-AC IR codes | Not yet found |
| **Shelly 2PM Gen4 blinds (Zigbee)** | Arriving Sunday — intended as the **Zigbee backbone**: blinds run the full length of the longest wall, so the Gen4 controllers act as Zigbee repeaters end-to-end |
| Mamad ceiling fan template | Needs its own fan type in `config/template/fans.yaml` — its remote differs from the other fans and is currently missing |
| Master-bath Arctic-fan window vent | Needs a Zigbee temp/humidity sensor + USB Zigbee-controlled plug |

---

## 4. Confirmed Facts

- **Qingping integration is Wi-Fi/cloud-MQTT** — no Bluetooth proxy needed. The Mamad unit just needs Wi-Fi (which reaches it through the mesh).
- **Fans are 433 MHz RF via Broadlink** — covers the whole house. No Sonoff RF Bridge needed.
- **Boiler is controlled via LocalTuya** (double-pole certified switch — not a cheap single-pole relay).

---

## 5. Dropped / Cancelled

| Project | Why dropped |
|---------|-------------|
| ESP32 "Totems" (bathroom sensor hubs) | No 3D printer → no clean enclosure; WAF = 0 |
| Mamad ESP32 Bluetooth proxy + Xiaomi BT thermometers | Superseded by Qingping Lite (Wi-Fi, no proxy needed) |
| Sonoff RF Bridge | Broadlink RM4 Pro covers the whole house via 433 MHz |
| Shelly 1PM light switches | Deprioritized / deferred |
| Shelly Plus 1PM bathroom-heater switches | Deprioritized / deferred |
| Zigbee toilet-paper roll buttons | Cancelled |

---

## 6. Automation Foundation

Automation design principles and patterns are documented in
[`docs/automation-architecture.md`](automation-architecture.md).

The full list of automations (autogenerated) is in [`docs/automations.md`](automations.md).

---

## 7. Future Automation Backlog

Rough priority order; each item becomes its own spec doc when actioned.

1. **Climate flagship** — air-quality-driven air-purifier control, replacing the current daily schedule; add a window-vs-AC call-to-action based on outdoor air quality.
2. **IR code-set finder script** — records via Broadlink IR-learn, searches the integration's IR database to identify the right code set; unblocks main-AC control.
3. **CUPS print server** — Brother USB laser → LAN, hosted on `blacky`.
4. **Remote access** — Nabu Casa vs Cloudflare Tunnel; decision pending.
5. **Window-open sensing** — current Zigbee contact sensors are too thick for the sliding-pane gap; needs an alternative form factor.
6. **Boiler cavity temp probe** — Dolphin-style internal water-temp sensor feeding a "water is hot" notification.
7. **Plant-care soil-moisture reminders** — 8 sensors ordered, 2 paired; complete pairing + build watering-reminder automations.
8. **Finish blacky Wake-on-LAN setup** — incomplete.
9. **Complete router MAC reservations** — also feeds Wi-Fi presence detection.
10. **blacky config ownership + privilege hardening** *(infra spec)* — `config/` is
    `root:root` because the official HA image runs as root (can't cleanly run non-root),
    which blocks the Mac→blacky `git pull` for tracked config files. Fix:
    `sudo chown -R yossi:yossi config` — HA-as-root still writes fine, and the files HA
    creates as root (`.storage/`, `*.db`, `*.log`, `secrets.yaml`) are gitignored so their
    ownership is moot; automations are git-authored (not via the HA UI) so HA only reads
    them. **This chown must be added to the OS setup script (`setup.sh`)** so it is applied
    on every rebuild. Also **drop `privileged: true`** from the `homeassistant` compose
    service — HA uses no USB/BT/`devices` (the Zigbee dongle belongs to the `zigbee2mqtt`
    container; host networking doesn't need privileged); recreating the container is a brief
    HA restart.
11. **Reconcile blacky git drift** *(part of #10)* — live config on blacky is not fully in
    git: `zigbee2mqtt/config/configuration.yaml` is tracked but has uncommitted local edits;
    `config/custom_components/ar_smart_ir/` and `config/custom_components/qingping_cgs1/` are
    **untracked** (HACS-installed post-init, not backed up — `ar_smart_ir` likely feeds the
    IR-finder task). Commit the z2m edit and decide track-vs-gitignore for the HACS
    components so git is the real source of truth.

---

## 8. Decision Log

| # | Decision |
|---|----------|
| 1 | Boiler switch must be double-pole and certified — no cheap single-pole relays (fire hazard, code violation). |
| 2 | IR requires line-of-sight per room; 433 MHz RF passes through walls except Mamad concrete. |
| 3 | Mamad is a Faraday cage — Wi-Fi backbone (Asus mesh) solves the connectivity problem; no RF or BT proxy needed for devices that support Wi-Fi. |
| 4 | Server runs Debian 13 headless + Docker — no desktop; Ubuntu Desktop was wasting RAM on a GUI nobody looks at. |
| 5 | No 3D printer available — all physical mounts use off-the-shelf materials (PVC foam board, VHB tape, Dual Lock, zip ties). |
